local _version="1.6.64-fix"
local WindUI=loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/download/".._version.."/main.lua"))()WindUI:AddTheme({Name="JHyellow",Primary=Color3.fromHex("#a78bfa"),White=Color3.fromHex("#a78bfa"),Black=Color3.new(0,0,0),Dialog=Color3.fromHex("#18181b"),Background=Color3.fromHex("#000000"),BackgroundTransparency=0,Hover=Color3.fromHex("#FFFFFF"),PanelBackground=Color3.new(1,1,1),PanelBackgroundTransparency=0.95,WindowBackground=Color3.fromHex("#0a0a0a"),WindowShadow=Color3.new(0,0,0),WindowShadowTransparency=0.6,WindowTopbarTitle=Color3.fromHex("#FFFFFF"),WindowTopbarAuthor=Color3.fromHex("#a6e3a1"),WindowTopbarIcon=Color3.fromHex("#a1a1aa"),WindowTopbarButtonIcon=Color3.fromHex("#a6e3a1"),WindowSearchBarBackground=Color3.fromHex("#0a0a0a"),TabBackground=Color3.fromHex("#a78bfa"),TabBackgroundHover=Color3.fromHex("#a78bfa"),TabBackgroundHoverTransparency=0.97,TabBackgroundActive=Color3.fromHex("#a78bfa"),TabBackgroundActiveTransparency=0.93,TabText=Color3.fromHex("#a78bfa"),TabTextTransparency=0.3,TabTextTransparencyActive=0,TabTitle=Color3.fromHex("#ffffff"),TabIcon=Color3.fromHex("#a78bfa"),TabIconTransparency=0.4,TabIconTransparencyActive=0,TabBorderTransparency=1,TabBorderTransparencyActive=0.65,TabBorder=Color3.fromHex("#a78bfa"),ElementBackground=Color3.fromHex("#FFFFFF"),ElementBackgroundTransparency=0.93,ElementBackgroundHover=WindUI.Creator:AddColor("ElementBackground","#ffffff",1/10),ElementTitle=Color3.fromHex("#FFFFFF"),ElementDesc=Color3.fromHex("#FFFFFF"),ElementIcon=Color3.fromHex("#a1a1aa"),PopupBackground=Color3.fromHex("#0a0a0a"),PopupBackgroundTransparency="BackgroundTransparency",PopupTitle=Color3.fromHex("#FFFFFF"),PopupContent=Color3.fromHex("#FFFFFF"),PopupIcon=Color3.fromHex("#a1a1aa"),DialogBackground=Color3.fromHex("#0a0a0a"),DialogBackgroundTransparency="BackgroundTransparency",DialogTitle=Color3.fromHex("#FFFFFF"),DialogContent=Color3.fromHex("#FFFFFF"),DialogIcon=Color3.fromHex("#a1a1aa"),Toggle=Color3.fromHex("#a78bfa"),ToggleBar=Color3.new(1,1,1),SliderIcon=Color3.fromHex("#a1a1aa"),Slider=Color3.fromHex("#a78bfa"),SliderThumb=Color3.new(1,1,1),SliderIconFrom=Color3.fromHex("#a1a1aa"),SliderIconTo=Color3.fromHex("#a1a1aa"),TabSectionIcon=Color3.fromHex("#a1a1aa"),SectionIcon=Color3.fromHex("#a1a1aa"),SectionExpandIcon=Color3.new(1,1,1),SectionExpandIconTransparency=0.4,SectionBox=Color3.new(1,1,1),SectionBoxTransparency=0.95,SectionBoxBorder=Color3.new(1,1,1),SectionBoxBorderTransparency=0.65,SectionBoxBackground=Color3.new(1,1,1),SectionBoxBackgroundTransparency=0.95,Notification=Color3.fromHex("#0a0a0a"),NotificationTitle=Color3.fromHex("#FFFFFF"),NotificationTitleTransparency=0,NotificationContent=Color3.fromHex("#FFFFFF"),NotificationContentTransparency=0.4,NotificationDuration=Color3.fromHex("#a78bfa"),NotificationDurationTransparency=0.95,NotificationBorder=Color3.fromHex("#a78bfa"),NotificationBorderTransparency=0.75,DropdownTabBorder=Color3.fromHex("#a78bfa"),LabelBackground=Color3.fromHex("#ffffff"),LabelBackgroundTransparency=0.95,})local Window=WindUI:CreateWindow({Title="JinHub | Dig & Clean",Icon="rbxassetid://75269568091825",Author="Website: jinhub.my.id/topscript",Folder="JinHub Config",Size=UDim2.fromOffset(480,350),MinSize=Vector2.new(480,350),MaxSize=Vector2.new(480,350),Transparent=true,Theme="JHyellow",Resizable=true,SideBarWidth=165,HideSearchBar=true,ScrollBarEnabled=true,NewElements=true,Radius=11,ElementsRadius=9,BackgroundImageTransparency=0.42,Background="rbxassetid://1234",})Window:DisableTopbarButtons({"Fullscreen",})Window:SetIconSize(53)local Players=game:GetService("Players")local VirtualUser=game:GetService("VirtualUser")local LocalPlayer=Players.LocalPlayer
local Connection
local function AntiAfk()if Connection then
Connection:Disconnect()end
Connection=LocalPlayer.Idled:Connect(function()pcall(function()VirtualUser:CaptureController()VirtualUser:ClickButton2(Vector2.new(0,0))end)end)end
AntiAfk()LocalPlayer.CharacterAdded:Connect(function()task.wait(300)AntiAfk()end)print("~Thanks For Use JinHub Script~")local CoreGui=game:GetService("CoreGui")local UserInputService=game:GetService("UserInputService")local gui=Instance.new("ScreenGui")gui.Name="JinHubXMenuGUI"
gui.IgnoreGuiInset=true
gui.ResetOnSpawn=false
gui.Parent=CoreGui
local button=Instance.new("ImageButton")button.Size=UDim2.new(0,44,0,44)button.Name="JinHubXToggle"
button.BackgroundColor3=Color3.fromRGB(13,13,13)button.Image="rbxassetid://75269568091825"
button.AutoButtonColor=true
button.Parent=gui
local corner=Instance.new("UICorner",button)corner.CornerRadius=UDim.new(0,6)local stroke=Instance.new("UIStroke")stroke.Thickness=1.3
stroke.Color=Color3.fromHex("#a78bfa")stroke.ApplyStrokeMode=Enum.ApplyStrokeMode.Border
stroke.Parent=button
local gradient=Instance.new("UIGradient")gradient.Color=ColorSequence.new({ColorSequenceKeypoint.new(0,Color3.fromRGB(158,124,249)),ColorSequenceKeypoint.new(1,Color3.fromRGB(201,75,241)),})gradient.Rotation=45
gradient.Parent=stroke
local BTN_SIZE=44
local TOP_PADDING=45
local function setDefaultPosition()button.Position=UDim2.new(0.5,-(BTN_SIZE/2),0,TOP_PADDING)end
setDefaultPosition()workspace.CurrentCamera:GetPropertyChangedSignal("ViewportSize"):Connect(setDefaultPosition)local dragging=false
local dragInput=nil
local dragStart=nil
local startPos=nil
local function clampPosition(px,py)local screenSize=workspace.CurrentCamera.ViewportSize
px=math.clamp(px,0,screenSize.X-BTN_SIZE)py=math.clamp(py,0,screenSize.Y-BTN_SIZE)return UDim2.new(0,px,0,py)end
button.InputBegan:Connect(function(input)if input.UserInputType==Enum.UserInputType.MouseButton1 or input.UserInputType==Enum.UserInputType.Touch then
dragging=true
dragStart=input.Position
startPos=button.Position
input.Changed:Connect(function()if input.UserInputState==Enum.UserInputState.End then
dragging=false
end
end)end
end)button.InputChanged:Connect(function(input)if input.UserInputType==Enum.UserInputType.MouseMovement or input.UserInputType==Enum.UserInputType.Touch then
dragInput=input
end
end)UserInputService.InputChanged:Connect(function(input)if input==dragInput and dragging then
local screenSize=workspace.CurrentCamera.ViewportSize
local delta=input.Position-dragStart
local px=startPos.X.Scale*screenSize.X+startPos.X.Offset+delta.X
local py=startPos.Y.Scale*screenSize.Y+startPos.Y.Offset+delta.Y
button.Position=clampPosition(px,py)end
end)local opened=true
button.MouseButton1Click:Connect(function()if opened then
opened=false
pcall(function()Window:Close()end)else
opened=true
pcall(function()Window:Open()end)end
end)Window:OnDestroy(function()gui:Destroy()end)Window:Tag({Title="v1.0.0",Color=Color3.fromHex("#a78bfa")})InfoTab=Window:Tab({Title="Local",Icon="solar:home-2-bold",IconColor=Color3.fromHex("#a78bfa"),IconThemed=true,ShowTabTitle=true,Border=true,})MainTab=Window:Tab({Title="Main",Icon="users-round",IconColor=Color3.fromHex("#a78bfa"),IconThemed=true,ShowTabTitle=true,Border=true,})MiscTab=Window:Tab({Title="Misc",Icon="sparkles",IconColor=Color3.fromHex("#a78bfa"),IconThemed=true,ShowTabTitle=true,Border=true,})ShopTab=Window:Tab({Title="Shop",Icon="coins",IconColor=Color3.fromHex("#a78bfa"),IconThemed=true,ShowTabTitle=true,Border=true,})Window:Divider()UtilityTab=Window:Tab({Title="Utility",Icon="solar:checklist-minimalistic-bold",IconColor=Color3.fromHex("#a78bfa"),IconThemed=true,ShowTabTitle=true,})ConfigTab=Window:Tab({Title="Config",Icon="solar:folder-2-bold",IconColor=Color3.fromHex("#a78bfa"),IconThemed=true,ShowTabTitle=true,Border=true,})InfoTab:Paragraph({Title="🌟 Welcome to JinHub",Desc="Thank you for using JinHub Script for Dig & Clean. This script is actively maintained and will continue to receive updates with new features and bug fixes. Your support means everything!",Image="",Thumbnail="",ImageSize=-15,ThumbnailSize=7,Locked=false,Buttons={},})InfoTab:Divider()InfoTab:Paragraph({Title="Join Our Discord",Desc="Buy Premium, Get the latest updates, report bugs, and connect with other users.",Image="rbxassetid://83278450537116",Thumbnail="",ImageSize=30,ThumbnailSize=-20,Locked=false,Buttons={{Icon="link-2",Title="Copy Invite Link",ImageSize=40,Callback=function()if setclipboard then
setclipboard("https://discord.gg/XPdhVZTaNv")end
WindUI:Notify({Title="Link Copied!",Content="Discord invite link has been copied to clipboard.",Duration=4,Icon="link-2",})end,},},})InfoTab:Divider()InfoTab:Paragraph({Title="Version Info",Desc="Version: v1.0.0\nGame: Dig & Clean\nAuthor: Changhoo\nWebsite: jinhub.my.id/topscript",Image="rbxassetid://76078495178149",Thumbnail="",ImageSize=30,ThumbnailSize=7,Locked=false,Buttons={},})InfoTab:Divider()InfoTab:Paragraph({Title="Found a Bug?",Desc="If you encounter any issues or bugs while using this script, please report them directly on our Discord server. Our team will respond as soon as possible.",Image="rbxassetid://89965059528921",Thumbnail="",ImageSize=30,ThumbnailSize=7,Locked=false,Buttons={},})InfoTab:Divider()InfoTab:Paragraph({Title="Disclaimer",Desc="This script is made for educational purposes only. Use at your own risk. JinHub is not responsible for any consequences resulting from the use of this script.",Image="rbxassetid://140438367956051",Thumbnail="",ImageSize=30,ThumbnailSize=7,Locked=false,Buttons={},})local Players=game:GetService("Players")local RunService=game:GetService("RunService")local CollectionService=game:GetService("CollectionService")local ReplicatedStorage=game:GetService("ReplicatedStorage")local LocalPlayer=Players.LocalPlayer
local MyGen=(getgenv().AutoDetectorGen or 0)+1
getgenv().AutoDetectorGen=MyGen
local function IsCurrentGen()return getgenv().AutoDetectorGen==MyGen
end
local function findClassTable(className)for _,obj in ipairs(getgc(true))do
if type(obj)=="table" then
local ok,mt=pcall(getmetatable,obj)if ok and type(mt)=="table" and rawget(mt,"__tostring")then
local ok2,str=pcall(tostring,obj)if ok2 and str==className then
if rawget(obj,"new")~=nil then
return obj
end
end
end
end
end
return nil
end
local function findInstanceOf(classTable,sanityField)if not classTable then
return nil
end
for _,obj in ipairs(getgc(true))do
if type(obj)=="table" and obj~=classTable then
local ok,mt=pcall(getmetatable,obj)if ok and mt==classTable then
if not sanityField or rawget(obj,sanityField)~=nil then
return obj
end
end
end
end
return nil
end
local function findTableWithKeys(keys)for _,obj in ipairs(getgc(true))do
if type(obj)=="table" then
local matches=true
for _,k in ipairs(keys)do
if rawget(obj,k)==nil then
matches=false
break
end
end
if matches then
return obj
end
end
end
return nil
end
local DigControllerClass=findClassTable("DigController")local SurfacedItemControllerClass=findClassTable("SurfacedItemController")local IslandControllerClass=findClassTable("IslandController")local digInst=nil
local sweepInst=nil
local surfacedItemInst=nil
local DetectorEvents=nil
local ShovelEvents=nil
local ShovelFunctions=nil
local islandsInst=nil
local function ensureAllResolved()if not digInst then
digInst=findInstanceOf(DigControllerClass,"affordNotification")if digInst then
sweepInst=digInst.sweep
end
end
if not surfacedItemInst then
surfacedItemInst=findInstanceOf(SurfacedItemControllerClass,"pending")end
if not DetectorEvents then
DetectorEvents=findTableWithKeys({"SetDetectorHeld","BuriedNodes"})end
if not ShovelEvents then
ShovelEvents=findTableWithKeys({"SetShovelEquipped","DigReady","DigInput"})end
if not ShovelFunctions then
ShovelFunctions=findTableWithKeys({"BeginDig","ResolveDig","StopPower"})end
if not islandsInst then
islandsInst=findInstanceOf(IslandControllerClass,"islands")end
return digInst~=nil and sweepInst~=nil
and DetectorEvents~=nil and ShovelEvents~=nil and ShovelFunctions~=nil
end
local IslandsList=nil
local IslandsFetchStarted=false
local getIslandsFolderFn=nil
local GetIslandsFolderResolveAttempted=false
local function ensureGetIslandsFolderResolved()if getIslandsFolderFn then
return true
end
if GetIslandsFolderResolveAttempted then
return false
end
GetIslandsFolderResolveAttempted=true
local okFind,mod=pcall(function()return ReplicatedStorage:WaitForChild("TS",5):WaitForChild("utils",5):WaitForChild("world",5):WaitForChild("getStarterIsland",5)end)if not okFind or not mod then
return false
end
local okReq,result=pcall(require,mod)if not okReq or type(result)~="table" or type(result.getIslandsFolder)~="function" then
return false
end
getIslandsFolderFn=result.getIslandsFolder
return true
end
local function ensureIslandsListFetched()if IslandsList then
return true
end
if not islandsInst then
return false
end
if IslandsFetchStarted then
return false
end
IslandsFetchStarted=true
task.spawn(function()local ok,list=pcall(function()return islandsInst:getIslands()end)if ok and list and #list>0 then
IslandsList=list
else
IslandsFetchStarted=false
end
end)return false
end
local IslandModelCache={}local function getIslandModel(islandId)if not islandId then
return nil
end
if IslandModelCache[islandId]~=nil then
local cached=IslandModelCache[islandId]if cached==false then
return nil
end
return cached
end
if not ensureGetIslandsFolderResolved()or not IslandsList then
return nil
end
local islandsFolder=getIslandsFolderFn()if not islandsFolder then
return nil
end
local entry=nil
for _,isl in ipairs(IslandsList)do
if isl.id==islandId then
entry=isl
break
end
end
if not entry or not entry.name then
IslandModelCache[islandId]=false
return nil
end
local model=islandsFolder:FindFirstChild(entry.name)IslandModelCache[islandId]=model or false
return model
end
local function nearestIslandIdForPosition(pos)if not IslandsList or not pos then
return nil
end
local bestId,bestDistSq=nil,nil
for _,isl in ipairs(IslandsList)do
if isl.spawn then
local dx=isl.spawn.Position.X-pos.X
local dz=isl.spawn.Position.Z-pos.Z
local distSq=dx*dx+dz*dz
if not bestDistSq or distSq<bestDistSq then
bestDistSq=distSq
bestId=isl.id
end
end
end
return bestId
end
local function getCurrentIslandId()if not islandsInst then
return nil
end
local ok,id=pcall(function()return islandsInst:getCurrentIslandId()end)if not ok then
return nil
end
return id
end
local DIG_ZONE_TAG=nil
local DigZoneTagResolveAttempted=false
local function ensureDigZoneTagResolved()if DIG_ZONE_TAG then
return true
end
if DigZoneTagResolveAttempted then
return false
end
DigZoneTagResolveAttempted=true
local okFind,mod=pcall(function()return ReplicatedStorage:WaitForChild("TS",5):WaitForChild("constants",5):WaitForChild("digging",5):WaitForChild("Shovels",5)end)if not okFind or not mod then
return false
end
local okReq,result=pcall(require,mod)if not okReq or type(result)~="table" or type(result.DIG_ZONE_TAG)~="string" then
return false
end
DIG_ZONE_TAG=result.DIG_ZONE_TAG
return true
end
local function randomPointInPart(part,awayFromWorldPos)local size=part.Size
local radiusX=size.X*0.5*1.25
local radiusZ=size.Z*0.5*1.25
if awayFromWorldPos then
local ok,localPlayerPos=pcall(function()return part.CFrame:PointToObjectSpace(awayFromWorldPos)end)if ok and localPlayerPos then
local awayDirX=(localPlayerPos.X>=0)and-1 or 1
local awayDirZ=(localPlayerPos.Z>=0)and-1 or 1
local offsetX=awayDirX*(0.5+math.random()*0.5)*radiusX
local offsetZ=awayDirZ*(0.5+math.random()*0.5)*radiusZ
return part.CFrame:PointToWorldSpace(Vector3.new(offsetX,0,offsetZ))end
end
local localOffset=Vector3.new((math.random()*2-1)*radiusX,0,(math.random()*2-1)*radiusZ)return part.CFrame:PointToWorldSpace(localOffset)end
local ExploreArriveDist=8
local ExploreVisitCooldown=20
local ExploreMaxStaleSeconds=60
local exploreTargetPart=nil
local exploreTargetPos=nil
local exploreTargetPickedAt=0
local exploreVisitedAt={}local function pickExploreTarget(hrp)if not ensureDigZoneTagResolved()then
return nil
end
local now=os.clock()local currentIslandId=getCurrentIslandId()local currentIslandModel=currentIslandId and getIslandModel(currentIslandId)or nil
if exploreTargetPart and currentIslandModel and not exploreTargetPart:IsDescendantOf(currentIslandModel)then
exploreTargetPart=nil
exploreTargetPos=nil
end
if exploreTargetPart and exploreTargetPart.Parent and exploreTargetPos then
local dist=(exploreTargetPos-hrp.Position).Magnitude
if dist>ExploreArriveDist and(now-exploreTargetPickedAt)<ExploreMaxStaleSeconds then
return exploreTargetPos
end
exploreVisitedAt[exploreTargetPart]=now
exploreTargetPart=nil
exploreTargetPos=nil
end
local okTagged,tagged=pcall(function()return CollectionService:GetTagged(DIG_ZONE_TAG)end)if not okTagged or not tagged or #tagged==0 then
return nil
end
local best,bestScore=nil,nil
for _,part in ipairs(tagged)do
if part:IsA("BasePart")then
local belongsToCurrentIsland=not currentIslandModel or part:IsDescendantOf(currentIslandModel)if belongsToCurrentIsland then
local lastVisited=exploreVisitedAt[part]local isCoolingDown=lastVisited~=nil and(now-lastVisited)<ExploreVisitCooldown
if not isCoolingDown then
local dist=(part.Position-hrp.Position).Magnitude
local neverVisitedBonus=lastVisited and 0 or 100000
local score=dist+neverVisitedBonus
if not bestScore or score>bestScore then
best=part
bestScore=score
end
end
end
end
end
if not best then
local oldestPart,oldestTime=nil,math.huge
for _,part in ipairs(tagged)do
if part:IsA("BasePart")then
local belongsToCurrentIsland=not currentIslandModel or part:IsDescendantOf(currentIslandModel)if belongsToCurrentIsland then
local lastVisited=exploreVisitedAt[part]or 0
if lastVisited<oldestTime then
oldestPart=part
oldestTime=lastVisited
end
end
end
end
best=oldestPart
end
if not best then
return nil
end
exploreTargetPart=best
exploreTargetPos=randomPointInPart(best,hrp.Position)exploreTargetPickedAt=now
return exploreTargetPos
end
local RARITY_ORDER={"common","uncommon","rare","epic","legendary","mythic","divine","eternal","transcendent",}local function capitalize(s)return s:sub(1,1):upper()..s:sub(2)end
local RarityLabels={}local RarityDropdownValues={}for _,rarity in ipairs(RARITY_ORDER)do
local label=capitalize(rarity)table.insert(RarityDropdownValues,label)RarityLabels[label]=rarity
end
local AutoDig={}AutoDig.WalkEnabled=false
AutoDig.DigEnabled=false
AutoDig.FarmMethod="Walk"
AutoDig.SelectedRarities={}AutoDig.PrioritizeSurfacedSpawn=false
local State={DigMaxSeconds=6}local IsDigging=false
local CurrentDigToken=nil
local function getCharacter()local char=LocalPlayer.Character
if not char then
return nil
end
local hrp=char:FindFirstChild("HumanoidRootPart")local hum=char:FindFirstChildOfClass("Humanoid")if not hrp or not hum or hum.Health<=0 then
return nil
end
return char,hum,hrp
end
local lastHeldState=nil
local function EnsureDetectorHeld(value)if not DetectorEvents then return end
if lastHeldState==value then return end
lastHeldState=value
pcall(function()DetectorEvents.SetDetectorHeld:fire(value)end)end
local function nodePassesRarityFilter(node)if next(AutoDig.SelectedRarities)==nil then
return true
end
local rarity=node and node.rarity
if not rarity then
return true
end
return AutoDig.SelectedRarities[rarity]==true
end
local function nodeBelongsToCurrentIsland(pos)local currentIslandId=getCurrentIslandId()if not currentIslandId then
return true
end
local nodeIslandId=nearestIslandIdForPosition(pos)if not nodeIslandId then
return true
end
return nodeIslandId==currentIslandId
end
local function nearestKnownNode(position)local best,bestDist=nil,nil
for _,node in pairs(sweepInst.nodes)do
if nodePassesRarityFilter(node)and node.position and nodeBelongsToCurrentIsland(node.position)then
local dist=(node.position-position).Magnitude
if not bestDist or dist<bestDist then
best=node
bestDist=dist
end
end
end
return best,bestDist
end
local function findReadyDigSpot(hrp)local okSpot,spot=pcall(function()return sweepInst:nearestSpot(hrp.Position)end)if okSpot and spot and nodePassesRarityFilter(spot)then
if not spot.position or nodeBelongsToCurrentIsland(spot.position)then
return "buried",spot
end
end
if surfacedItemInst and next(AutoDig.SelectedRarities)==nil then
local okReach,isReach=pcall(function()return surfacedItemInst:hasDigSpotNear(hrp.Position)end)if okReach and isReach then
return "surfaced",true
end
end
return nil,nil
end
local function nearestSurfacedSpawn(position)if not surfacedItemInst or not surfacedItemInst.scenes then
return nil,nil
end
local best,bestId,bestDist=nil,nil,nil
local ok=pcall(function()for id,scene in pairs(surfacedItemInst.scenes)do
local diggable=scene.retractingAt==nil
and scene.mound~=nil
and #scene.mound>0
if diggable and scene.surfaceCFrame and nodeBelongsToCurrentIsland(scene.surfaceCFrame.Position)then
local dist=(scene.surfaceCFrame.Position-position).Magnitude
if not bestDist or dist<bestDist then
best=scene
bestId=id
bestDist=dist
end
end
end
end)if not ok or not best then
return nil,nil
end
return{id=bestId,position=best.surfaceCFrame.Position,reach=best.reach or 0,},bestDist
end
local function moveToTarget(hrp,hum,targetPos)if AutoDig.FarmMethod=="Teleport" then
hrp.CFrame=CFrame.new(targetPos+Vector3.new(0,3,0))else
hum:MoveTo(targetPos)end
end
local DIG_PROGRESS_START=0.33
local DIG_WIN_THRESHOLD=0.985
local DIG_LOSE_THRESHOLD=0.0574
local DIG_INPUT_SEND_INTERVAL=0.1
local VIRTUAL_CLICKS_PER_SECOND=25
local function DoFullDig(labelId,labelRarity,digId)if IsDigging then return end
IsDigging=true
local myDigToken={}CurrentDigToken=myDigToken
task.delay(State.DigMaxSeconds+8,function()if IsDigging and CurrentDigToken==myDigToken then
IsDigging=false
lastHeldState=nil
end
end)local overrideConn
overrideConn=RunService.Heartbeat:Connect(function()pcall(function()DetectorEvents.SetDetectorHeld:fire(false)end)pcall(function()ShovelEvents.SetShovelEquipped:fire(true)end)end)local ok,err=pcall(function()if not ShovelFunctions or not ShovelEvents then
return
end
task.wait(0.25)local beginOk,session=pcall(function()if digId~=nil then
return ShovelFunctions.BeginDig:invoke(digId):expect()end
return ShovelFunctions.BeginDig:invoke():expect()end)if not beginOk or not session then
return
end
local sessionId=session.sessionId
local difficulty=session.difficulty
if not sessionId or not difficulty then
return
end
local clickPower=difficulty.clickPower
local decay=difficulty.decay
local progress=DIG_PROGRESS_START
local totalClicks=0
local lastInputSent=os.clock()local lastFrame=os.clock()local success=false
local deadline=os.clock()+State.DigMaxSeconds
while os.clock()<deadline do
local now=os.clock()local dt=now-lastFrame
lastFrame=now
progress=math.max(0,progress-decay*dt)local clicksThisFrame=math.max(1,math.floor(VIRTUAL_CLICKS_PER_SECOND*dt))for _=1,clicksThisFrame do
progress=math.min(1,progress+clickPower)totalClicks=totalClicks+1
end
if now-lastInputSent>=DIG_INPUT_SEND_INTERVAL then
lastInputSent=now
local inputOk=pcall(function()ShovelEvents.DigInput:fire(sessionId,totalClicks)end)if not inputOk then
break
end
end
if progress>=DIG_WIN_THRESHOLD then
success=true
break
elseif progress<=DIG_LOSE_THRESHOLD then
success=false
break
end
task.wait()end
pcall(function()ShovelEvents.DigInput:fire(sessionId,totalClicks)end)local resolveOk,finalResult=pcall(function()return ShovelFunctions.ResolveDig:invoke(sessionId,success,totalClicks):expect()end)if not resolveOk then
pcall(function()if WindUI then
WindUI:Notify({Title="Failed Dig",Content="Power shovel is not strong enough.",Icon="shovel",Duration=2.5,})end
end)return
end
if not finalResult then
pcall(function()if WindUI then
WindUI:Notify({Title="Failed Dig",Content="Power shovel is not strong enough.",Icon="shovel",Duration=2.5,})end
end)end
end)if overrideConn then
overrideConn:Disconnect()end
if CurrentDigToken==myDigToken then
lastHeldState=nil
task.wait(0.2)IsDigging=false
end
end
local lastStepAt=0
local STEP_INTERVAL=0.2
local function AutoDetectorStep()if not(AutoDig.WalkEnabled or AutoDig.DigEnabled)then
if not IsDigging then
EnsureDetectorHeld(false)end
return
end
if not ensureAllResolved()then
return
end
ensureIslandsListFetched()if IsDigging then
return
end
local char,hum,hrp=getCharacter()if not hrp then
return
end
EnsureDetectorHeld(true)if AutoDig.PrioritizeSurfacedSpawn then
local spawnSpot,spawnDist=nearestSurfacedSpawn(hrp.Position)if spawnSpot then
if spawnDist<=spawnSpot.reach then
hum:MoveTo(hrp.Position)if AutoDig.DigEnabled then
task.spawn(DoFullDig,"surfaced-spawn","priority",nil)end
elseif AutoDig.WalkEnabled then
moveToTarget(hrp,hum,spawnSpot.position)end
return
end
end
local kind,spot=findReadyDigSpot(hrp)if kind=="buried" then
hum:MoveTo(hrp.Position)if AutoDig.DigEnabled then
task.spawn(DoFullDig,spot.id,spot.rarity,spot.id)end
return
elseif kind=="surfaced" then
hum:MoveTo(hrp.Position)if AutoDig.DigEnabled then
task.spawn(DoFullDig,"surfaced-item","?",nil)end
return
end
if AutoDig.WalkEnabled then
local node=nearestKnownNode(hrp.Position)if node then
exploreTargetPart=nil
exploreTargetPos=nil
moveToTarget(hrp,hum,node.position)else
local explorePos=pickExploreTarget(hrp)if explorePos then
moveToTarget(hrp,hum,explorePos)end
end
end
end
local mainHeartbeatConn=nil
mainHeartbeatConn=RunService.Heartbeat:Connect(function()if not IsCurrentGen()then
if mainHeartbeatConn then
mainHeartbeatConn:Disconnect()end
return
end
local now=os.clock()if now-lastStepAt<STEP_INTERVAL then
return
end
lastStepAt=now
local ok=pcall(AutoDetectorStep)end)function AutoDig:SetWalkEnabled(state)self.WalkEnabled=state
end
function AutoDig:SetDigEnabled(state)self.DigEnabled=state
end
function AutoDig:SetFarmMethod(method)self.FarmMethod=method
end
function AutoDig:SetSelectedRarities(labelList)local set={}for _,label in ipairs(labelList or{})do
local rarity=RarityLabels[label]if rarity then
set[rarity]=true
end
end
self.SelectedRarities=set
end
MainTab:Section({Title="Auto Farm",Icon="magnet",IconColor=Color3.fromRGB(120,140,255),TextSize=18,Box=false,BoxBorder=true,Opened=false,})MainTab:Dropdown({Title="Metode Farm",Values={"Walk","Teleport"},Value="Walk",Multi=false,Flag="farm_method",Callback=function(selected)AutoDig:SetFarmMethod(selected)end})MainTab:Dropdown({Title="Rarity Filter",Values=RarityDropdownValues,Value={},Multi=true,Flag="rarity_filter",Callback=function(selected)AutoDig:SetSelectedRarities(selected)end})MainTab:Toggle({Title="Auto Dig",Flag="auto_dig",Callback=function(state)AutoDig:SetDigEnabled(state)end})MainTab:Toggle({Title="Auto Detector",Flag="auto_detector",Callback=function(state)AutoDig:SetWalkEnabled(state)end})MainTab:Toggle({Title="Prioritize spawned items.",Flag="auto_detector_prioritize_surfaced_spawn",Callback=function(state)AutoDig.PrioritizeSurfacedSpawn=state
end})MainTab:Divider()local Players=game:GetService("Players")local ReplicatedStorage=game:GetService("ReplicatedStorage")local CollectionService=game:GetService("CollectionService")local LocalPlayer=Players.LocalPlayer
getgenv().AutoSpray=getgenv().AutoSpray or{Enabled=false,Delay=0.5,BeginArg=3,SelectedRarities={},}local State=getgenv().AutoSpray
local RARITY_ORDER={"common","uncommon","rare","epic","legendary","mythic","divine","eternal","transcendent",}local function Capitalize(s)return s:sub(1,1):upper()..s:sub(2)end
local RarityLabels={}local RarityDropdownValues={}for _,rarity in ipairs(RARITY_ORDER)do
local label=Capitalize(rarity)table.insert(RarityDropdownValues,label)RarityLabels[label]=rarity
end
local Items=nil
local ItemsResolveFailCount=0
local function ensureItemsResolved()if Items then
return true
end
if ItemsResolveFailCount>=3 then
return false
end
local okFind,mod=pcall(function()return ReplicatedStorage:WaitForChild("TS",5):WaitForChild("constants",5):WaitForChild("items",5):WaitForChild("Items",5)end)if not okFind or not mod then
ItemsResolveFailCount+=1
return false
end
local okReq,result=pcall(require,mod)if not okReq or type(result)~="table" then
ItemsResolveFailCount+=1
return false
end
Items=result.Items or result
return Items~=nil
end
local function GetToolRarity(tool)if not ensureItemsResolved()then
return nil
end
local itemId=tool:GetAttribute("itemId")if type(itemId)~="string" then
return nil
end
local itemData=Items[itemId]if not itemData then
return nil
end
return itemData.rarity
end
local function ToolPassesRarityFilter(tool)if next(State.SelectedRarities)==nil then
return true
end
local rarity=GetToolRarity(tool)if not rarity then
return true
end
return State.SelectedRarities[rarity]==true
end
local GlobalItemsFunctions=ReplicatedStorage:FindFirstChild("shared/network/ItemsNetwork@GlobalItemsFunctions")local GlobalItemsEvents=ReplicatedStorage:FindFirstChild("shared/network/ItemsNetwork@GlobalItemsEvents")local function IsDirtyTool(tool)if not CollectionService:HasTag(tool,"Dirt")then
return false
end
return tool:GetAttribute("dirty")~=false
end
local function GetDirtyTools()local results={}local seen={}local function scan(container)if not container then
return
end
for _,tool in ipairs(container:GetChildren())do
if tool:IsA("Tool")then
local inventoryId=tool:GetAttribute("inventoryId")if type(inventoryId)=="string" and inventoryId~="" and not seen[inventoryId]then
if IsDirtyTool(tool)and ToolPassesRarityFilter(tool)then
seen[inventoryId]=true
table.insert(results,{tool=tool,inventoryId=inventoryId})end
end
end
end
end
local backpack=LocalPlayer:FindFirstChildOfClass("Backpack")scan(backpack)scan(LocalPlayer.Character)return results
end
local function TryClean(inventoryId)if not GlobalItemsFunctions or not GlobalItemsEvents then
return
end
local beginRemote=GlobalItemsFunctions:FindFirstChild("beginCleaning")local finishRemote=GlobalItemsEvents:FindFirstChild("finishCleaning")if not beginRemote or not finishRemote then
return
end
local ok=pcall(function()beginRemote:FireServer(State.BeginArg,inventoryId)end)if not ok then
return
end
task.wait(0.2)pcall(function()finishRemote:FireServer(inventoryId)end)end
task.spawn(function()while true do
task.wait(State.Delay)if not State.Enabled then
continue
end
local dirtyTools=GetDirtyTools()if #dirtyTools==0 then
continue
end
for _,entry in ipairs(dirtyTools)do
if not State.Enabled then
break
end
TryClean(entry.inventoryId)task.wait(State.Delay)end
end
end)MainTab:Dropdown({Title="Rarity Filter",Values=RarityDropdownValues,Value={},Multi=true,Locked=false,Flag="auto_spray_rarity_filter",Callback=function(selected)local set={}for _,label in ipairs(selected)do
local rarity=RarityLabels[label]if rarity then
set[rarity]=true
end
end
State.SelectedRarities=set
end,})MainTab:Toggle({Title="Auto Cleaning",Desc="Instant Spray",Default=State.Enabled,Flag="auto_spray_toggle",Callback=function(value)State.Enabled=value
end,})local Players=game:GetService("Players")local RunService=game:GetService("RunService")local ReplicatedStorage=game:GetService("ReplicatedStorage")local LocalPlayer=Players.LocalPlayer
if _G.__AutoSellInstance then
pcall(function()_G.__AutoSellInstance:Stop()end)_G.__AutoSellInstance=nil
end
local RARITY_ORDER={"common","uncommon","rare","epic","legendary","mythic","divine","eternal","transcendent",}local function safeWaitForChild(parent,name,timeout)if not parent then
return nil
end
local ok,child=pcall(function()return parent:WaitForChild(name,timeout or 3)end)if ok then
return child
end
return nil
end
do
local playerScripts=safeWaitForChild(LocalPlayer,"PlayerScripts",3)local ts=safeWaitForChild(playerScripts,"TS",3)local constants=safeWaitForChild(ts,"constants",3)local ui=safeWaitForChild(constants,"ui",3)local rarityStylesModule=safeWaitForChild(ui,"RarityStyles",3)if rarityStylesModule then
local ok,mod=pcall(require,rarityStylesModule)if ok and mod and mod.RARITY_ORDER then
RARITY_ORDER=mod.RARITY_ORDER
end
end
end
local function capitalize(s)return s:sub(1,1):upper()..s:sub(2)end
local RarityLabels={}local RarityDropdownValues={}for _,rarity in ipairs(RARITY_ORDER)do
local label=capitalize(rarity)table.insert(RarityDropdownValues,label)RarityLabels[label]=rarity
end
local Items=nil
do
local ts=safeWaitForChild(ReplicatedStorage,"TS",3)local constants=safeWaitForChild(ts,"constants",3)local itemsFolder=safeWaitForChild(constants,"items",3)local itemsModule=safeWaitForChild(itemsFolder,"Items",3)if itemsModule then
local ok,mod=pcall(require,itemsModule)if ok and mod then
Items=mod.Items
end
end
end
local SellFunctions=nil
local function ensureSellNetwork()if SellFunctions then
return true
end
local containers={}local playerScripts=safeWaitForChild(LocalPlayer,"PlayerScripts",3)local psTS=safeWaitForChild(playerScripts,"TS",3)if psTS then
table.insert(containers,psTS)end
local rsTS=safeWaitForChild(ReplicatedStorage,"TS",3)if rsTS then
table.insert(containers,rsTS)end
for _,container in ipairs(containers)do
local network=safeWaitForChild(container,"network",3)local sellNetworkModule=safeWaitForChild(network,"SellNetwork",3)if sellNetworkModule then
local ok,mod=pcall(require,sellNetworkModule)if ok and mod and mod.SellFunctions then
SellFunctions=mod.SellFunctions
return true
end
end
end
return false
end
local function findClassTable(className)for _,obj in ipairs(getgc(true))do
if type(obj)=="table" then
local ok,mt=pcall(getmetatable,obj)if ok and type(mt)=="table" and rawget(mt,"__tostring")then
local ok2,str=pcall(tostring,obj)if ok2 and str==className then
if rawget(obj,"new")~=nil then
return obj
end
end
end
end
end
return nil
end
local function findInstanceOf(classTable,sanityField)if not classTable then
return nil
end
for _,obj in ipairs(getgc(true))do
if type(obj)=="table" and obj~=classTable then
local ok,mt=pcall(getmetatable,obj)if ok and mt==classTable then
if not sanityField or rawget(obj,sanityField)~=nil then
return obj
end
end
end
end
return nil
end
local DataControllerClass=findClassTable("DataController")local dataInst=nil
local function ensureDataInstance()if not dataInst then
dataInst=findInstanceOf(DataControllerClass,nil)end
return dataInst~=nil
end
local function getInventory()if not ensureDataInstance()then
return nil
end
local ok,data=pcall(function()return dataInst:getDataIfLoaded()end)if not ok or not data then
return nil
end
return data.Inventory
end
local function getInventoryCount()local inventory=getInventory()if not inventory then
return 0
end
local count=0
for _,entry in pairs(inventory)do
if entry.pedestalSlot==nil and entry.polisherSlot==nil then
count=count+1
end
end
return count
end
local NPC_NEAR_DISTANCE=5
local NPC_TP_OFFSET=Vector3.new(3,0,0)local getStarterIslandFn=nil
local function resolveGetStarterIsland()if getStarterIslandFn then
return true
end
local ts=safeWaitForChild(ReplicatedStorage,"TS",3)local utils=safeWaitForChild(ts,"utils",3)local world=safeWaitForChild(utils,"world",3)local mod=safeWaitForChild(world,"getStarterIsland",3)if not mod then
return false
end
local ok,required=pcall(require,mod)if ok and required and required.getStarterIsland then
getStarterIslandFn=required.getStarterIsland
return true
end
return false
end
local npcRoot=nil
local function ensureNpcRoot()if npcRoot and npcRoot.Parent then
return npcRoot
end
if not resolveGetStarterIsland()then
return nil
end
local ok,island=pcall(getStarterIslandFn)if not ok or not island then
return nil
end
local npcs=safeWaitForChild(island,"NPCs",3)local sell=safeWaitForChild(npcs,"Sell",3)local sellerNpc=safeWaitForChild(sell,"SellerNPC",3)local hrp=safeWaitForChild(sellerNpc,"HumanoidRootPart",3)npcRoot=hrp
return npcRoot
end
local function isNearNpc(playerHrp)local root=ensureNpcRoot()if not root or not playerHrp then
return false
end
return(playerHrp.Position-root.Position).Magnitude<=NPC_NEAR_DISTANCE
end
local function teleportToNpc(playerHrp)local root=ensureNpcRoot()if not root or not playerHrp then
return false
end
playerHrp.CFrame=CFrame.new(root.Position+NPC_TP_OFFSET)return true
end
local AutoSell={}AutoSell.SellByRarityEnabled=false
AutoSell.SellAllEnabled=false
AutoSell.SelectedRarities={}AutoSell.SellTriggerCount=50
local heartbeatConn=nil
local sellBusy=false
local needsRetp=false
local SELL_LOOP_INTERVAL=1.5
local PER_ITEM_EQUIP_WAIT=0.5
local PER_ITEM_COOLDOWN=0.5
local SELL_MAX_RETRIES=4
local SELL_RETRY_DELAY=0.3
local function anyEnabled()return AutoSell.SellByRarityEnabled or AutoSell.SellAllEnabled
end
local function entryPassesFilter(entry)if not entry then
return false
end
if entry.pedestalSlot~=nil or entry.polisherSlot~=nil then
return false
end
if next(AutoSell.SelectedRarities)==nil then
return false
end
if not Items or not entry.id or not Items[entry.id]then
return false
end
local rarity=Items[entry.id].rarity
return AutoSell.SelectedRarities[rarity]==true
end
local function findToolForEntry(entry)local char=LocalPlayer.Character
local backpack=LocalPlayer:FindFirstChild("Backpack")local containers={}if char then
table.insert(containers,char)end
if backpack then
table.insert(containers,backpack)end
for _,container in ipairs(containers)do
for _,inst in ipairs(container:GetChildren())do
if inst:IsA("Tool")and inst:GetAttribute("inventoryId")==entry.uid then
return inst
end
end
end
return nil
end
local function sellOneEntry(entry)local char=LocalPlayer.Character
local hum=char and char:FindFirstChildOfClass("Humanoid")if not hum then
return false
end
local tool=findToolForEntry(entry)if not tool then
needsRetp=true
return false
end
for attempt=1,SELL_MAX_RETRIES do
if not tool.Parent then
needsRetp=true
return false
end
local equipOk=pcall(function()hum:EquipTool(tool)end)if not equipOk then
return false
end
task.wait(PER_ITEM_EQUIP_WAIT)local invokeOk,result=pcall(function()return SellFunctions.sellHeldItem:invoke()end)if invokeOk and result~=nil then
return true
end
if attempt<SELL_MAX_RETRIES then
task.wait(SELL_RETRY_DELAY)end
end
return false
end
local function sellAllAtOnce()pcall(function()SellFunctions.sellInventory:invoke()end)end
local function sellFilteredOneByOne()local inventory=getInventory()if not inventory then
return
end
for _,entry in ipairs(inventory)do
if not AutoSell.SellByRarityEnabled then
break
end
if entryPassesFilter(entry)then
sellOneEntry(entry)task.wait(PER_ITEM_COOLDOWN)end
end
end
local function step()if not anyEnabled()or sellBusy then
return
end
if not ensureSellNetwork()then
return
end
sellBusy=true
task.spawn(function()if AutoSell.SellAllEnabled then
sellAllAtOnce()task.wait(PER_ITEM_COOLDOWN)end
if AutoSell.SellByRarityEnabled and next(AutoSell.SelectedRarities)~=nil then
sellFilteredOneByOne()end
sellBusy=false
end)end
local lastRun=0
local function heartbeatStep()if not anyEnabled()then
return
end
if getInventoryCount()~=AutoSell.SellTriggerCount then
return
end
local char=LocalPlayer.Character
local hrp=char and char:FindFirstChild("HumanoidRootPart")if hrp then
if not isNearNpc(hrp)or needsRetp then
needsRetp=false
teleportToNpc(hrp)lastRun=os.clock()return
end
end
local now=os.clock()if now-lastRun<SELL_LOOP_INTERVAL then
return
end
lastRun=now
step()end
function AutoSell:Start()if heartbeatConn then
return
end
lastRun=0
heartbeatConn=RunService.Heartbeat:Connect(heartbeatStep)end
function AutoSell:Stop()if heartbeatConn then
heartbeatConn:Disconnect()heartbeatConn=nil
end
end
local function refreshRunning()if anyEnabled()then
AutoSell:Start()else
AutoSell:Stop()end
end
function AutoSell:SetSellByRarityEnabled(state)self.SellByRarityEnabled=state
refreshRunning()end
function AutoSell:SetSellAllEnabled(state)self.SellAllEnabled=state
refreshRunning()end
function AutoSell:SetSelectedRarities(labelList)local set={}for _,label in ipairs(labelList or{})do
local rarity=RarityLabels[label]if rarity then
set[rarity]=true
end
end
self.SelectedRarities=set
end
function AutoSell:SetSellTriggerCount(value)self.SellTriggerCount=value
end
ShopTab:Section({Title="Auto Selling",Icon="coins",IconColor=Color3.fromRGB(120,140,255),TextSize=18,})ShopTab:Dropdown({Title="Rarity Filter",Values=RarityDropdownValues,Value={},Multi=true,Flag="sell_rarity_filter",Callback=function(selected)AutoSell:SetSelectedRarities(selected)end})ShopTab:Slider({Title="Backpack Count",Value={Min=5,Max=50,Default=50},Step=1,Flag="sell_trigger_count",Callback=function(value)AutoSell:SetSellTriggerCount(value)end})ShopTab:Toggle({Title="Auto Sell By Rarity",Flag="auto_sell_rarity",Default=false,Callback=function(state)AutoSell:SetSellByRarityEnabled(state)end})ShopTab:Toggle({Title="Auto Sell All Item Inventory",Flag="auto_sell_all",Default=false,Callback=function(state)AutoSell:SetSellAllEnabled(state)end})_G.__AutoSellInstance=AutoSell
ShopTab:Divider()local Players=game:GetService("Players")local ReplicatedStorage=game:GetService("ReplicatedStorage")local LocalPlayer=Players.LocalPlayer
local ShopFunctions=nil
local DataFunctions=nil
local Shovels=nil
local Detectors=nil
local SprayBottles=nil
local getIslandsFolder=nil
local formatAbbrevMoney=nil
do
local PlayerScripts=LocalPlayer:WaitForChild("PlayerScripts")local TSFolder=PlayerScripts:WaitForChild("TS")local NetworkFolder=TSFolder:WaitForChild("network")local okShop,shopMod=pcall(function()return require(NetworkFolder:WaitForChild("ShopNetwork"))end)if okShop and shopMod then
ShopFunctions=shopMod.ShopFunctions
end
local okData,dataMod=pcall(function()return require(NetworkFolder:WaitForChild("DataNetwork"))end)if okData and dataMod then
DataFunctions=dataMod.DataFunctions
end
local ConstantsFolder=ReplicatedStorage:WaitForChild("TS"):WaitForChild("constants")local okShovels,shovelsMod=pcall(function()return require(ConstantsFolder:WaitForChild("digging"):WaitForChild("Shovels"))end)if okShovels and shovelsMod then
Shovels=shovelsMod.Shovels
else
end
local okDetectors,detectorsMod=pcall(function()return require(ConstantsFolder:WaitForChild("digging"):WaitForChild("Detectors"))end)if okDetectors and detectorsMod then
Detectors=detectorsMod.Detectors
else
end
local okSprays,spraysMod=pcall(function()return require(ConstantsFolder:WaitForChild("cleaning"):WaitForChild("SprayBottles"))end)if okSprays and spraysMod then
SprayBottles=spraysMod.SprayBottles
else
end
local UtilsFolder=ReplicatedStorage:WaitForChild("TS"):WaitForChild("utils")local okIslands,islandsMod=pcall(function()return require(UtilsFolder:WaitForChild("world"):WaitForChild("getStarterIsland"))end)if okIslands and islandsMod then
getIslandsFolder=islandsMod.getIslandsFolder
else
end
local okFormat,formatMod=pcall(function()return require(UtilsFolder:WaitForChild("formatting"):WaitForChild("formatAbbrevMoney"))end)if okFormat and formatMod then
formatAbbrevMoney=formatMod.formatAbbrevMoney
else
end
end
if not ShopFunctions then
return
end
if not DataFunctions then
end
local PlayerData=nil
local DATA_REFRESH_INTERVAL=3
local function refreshPlayerData()if not DataFunctions then
return
end
local ok,result=pcall(function()return DataFunctions.requestDataUpdate:invoke()end)if ok and result then
PlayerData=result
end
end
if DataFunctions then
task.spawn(function()for _=1,10 do
refreshPlayerData()if PlayerData then
break
end
task.wait(0.5)end
end)task.spawn(function()while true do
task.wait(DATA_REFRESH_INTERVAL)refreshPlayerData()end
end)end
local function isOwned(category,id)if not PlayerData then
return false
end
local list
if category=="shovel" then
list=PlayerData.OwnedShovels
elseif category=="spray" then
list=PlayerData.OwnedSprays
else
list=PlayerData.OwnedDetectors
end
if not list then
return false
end
return table.find(list,id)~=nil
end
local function formatPrice(cost)if formatAbbrevMoney then
local ok,formatted=pcall(formatAbbrevMoney,cost)if ok and formatted then
return formatted
end
end
return tostring(cost)end
local function buildBuyableItems(sourceTable)local items={}local labelToId={}if not sourceTable then
return items,labelToId
end
for id,def in pairs(sourceTable)do
if def.cost and def.cost~=0 then
table.insert(items,{id=id,displayName=def.displayName or id,cost=def.cost})end
end
table.sort(items,function(a,b)if a.cost~=b.cost then
return a.cost<b.cost
end
return a.displayName<b.displayName
end)local labels={}for _,item in ipairs(items)do
local label=("%s | %s"):format(item.displayName,formatPrice(item.cost))table.insert(labels,label)labelToId[label]=item.id
end
return labels,labelToId
end
local ShovelLabels,ShovelLabelToId=buildBuyableItems(Shovels)local DetectorLabels,DetectorLabelToId=buildBuyableItems(Detectors)local SprayLabels,SprayLabelToId=buildBuyableItems(SprayBottles)local CATEGORY_CONTAINER={shovel="BuyShovels",detector="BuyDetectors",spray="Sprays",}local function getDisplayName(category,id)if category=="shovel" then
return Shovels and Shovels[id]and Shovels[id].displayName
elseif category=="detector" then
return Detectors and Detectors[id]and Detectors[id].displayName
else
return SprayBottles and SprayBottles[id]and SprayBottles[id].displayName
end
end
local ModelCache={}local function findGearModel(category,id)local cacheKey=category.."/"..tostring(id)local cached=ModelCache[cacheKey]if cached and cached.Parent then
return cached
end
if not getIslandsFolder then
return nil
end
local displayName=getDisplayName(category,id)local containerName=CATEGORY_CONTAINER[category]if not displayName or not containerName then
return nil
end
local ok,islandsFolder=pcall(getIslandsFolder)if not ok or not islandsFolder then
return nil
end
for _,island in ipairs(islandsFolder:GetChildren())do
local npcs=island:FindFirstChild("NPCs")local gear=npcs and npcs:FindFirstChild("Gear")local container=gear and gear:FindFirstChild(containerName)if container then
local model=container:FindFirstChild(displayName,true)if model and model:IsA("Model")then
ModelCache[cacheKey]=model
return model
end
end
end
return nil
end
local function GetRoot()local Character=LocalPlayer.Character
if not Character then
return nil
end
return Character:FindFirstChild("HumanoidRootPart")end
local MODEL_APPROACH_OFFSET=Vector3.new(0,3,3)local function teleportToGearModel(category,id)local model=findGearModel(category,id)if not model then
return false
end
local rootPart=GetRoot()if not rootPart then
return false
end
local pivot=model:GetPivot()rootPart.CFrame=CFrame.new(pivot.Position+MODEL_APPROACH_OFFSET,pivot.Position)task.wait(0.2)return true
end
local DELAY_BETWEEN_ATTEMPTS=1.5
local function makeAutoBuyer(category,labelToId)local state={Enabled=false,SelectedLabels={},BoughtCache={},}task.spawn(function()while true do
task.wait(DELAY_BETWEEN_ATTEMPTS)if not state.Enabled then
continue
end
if #state.SelectedLabels==0 then
continue
end
for _,label in ipairs(state.SelectedLabels)do
if not state.Enabled then
break
end
local id=labelToId[label]local alreadyOwned=isOwned(category,id)or state.BoughtCache[id]if id and not alreadyOwned then
teleportToGearModel(category,id)local ok,promiseResult=pcall(function()return ShopFunctions.buyGear:invoke(category,id):catch(function()return false
end):expect()end)if ok and promiseResult==true then
state.BoughtCache[id]=true
task.spawn(refreshPlayerData)end
task.wait(0.3)end
end
end
end)return state
end
local ShovelBuyer=makeAutoBuyer("shovel",ShovelLabelToId)local DetectorBuyer=makeAutoBuyer("detector",DetectorLabelToId)local SprayBuyer=makeAutoBuyer("spray",SprayLabelToId)ShopTab:Section({Title="Gear Detector",Icon="magnet",IconColor=Color3.fromRGB(120,140,255),TextSize=18,Box=false,BoxBorder=true,Opened=false,})ShopTab:Dropdown({Title="Detector List",Values=DetectorLabels,Value={},Multi=true,Locked=false,Flag="auto_buy_detector_list",Callback=function(selected)DetectorBuyer.SelectedLabels=selected
end,})ShopTab:Toggle({Title="Auto Buy Detector",Flag="auto_buy_detector_toggle",Default=false,Callback=function(v)DetectorBuyer.Enabled=v
end,})ShopTab:Divider()ShopTab:Section({Title="Gear Shovel",Icon="shovel",IconColor=Color3.fromRGB(120,140,255),TextSize=18,Box=false,BoxBorder=true,Opened=false,})ShopTab:Dropdown({Title="Shovel List",Values=ShovelLabels,Value={},Multi=true,Locked=false,Flag="auto_buy_shovel_list",Callback=function(selected)ShovelBuyer.SelectedLabels=selected
end,})ShopTab:Toggle({Title="Auto Buy Shovel",Flag="auto_buy_shovel_toggle",Default=false,Callback=function(v)ShovelBuyer.Enabled=v
end,})ShopTab:Divider()ShopTab:Section({Title="Gear Spray",Icon="spray-can",IconColor=Color3.fromRGB(120,140,255),TextSize=18,Box=false,BoxBorder=true,Opened=false,})ShopTab:Dropdown({Title="Spray List",Values=SprayLabels,Value={},Multi=true,Locked=false,Flag="auto_buy_spray_list",Callback=function(selected)SprayBuyer.SelectedLabels=selected
end,})ShopTab:Toggle({Title="Auto Buy Spray",Flag="auto_buy_spray_toggle",Default=false,Callback=function(v)SprayBuyer.Enabled=v
end,})MiscTab:Section({Title="Auto Place",Icon="arrow-down-to-line",IconColor=Color3.fromRGB(120,140,255),TextSize=18,Box=false,BoxBorder=true,Opened=false,})local Players=game:GetService("Players")local ReplicatedStorage=game:GetService("ReplicatedStorage")local Workspace=game:GetService("Workspace")local LocalPlayer=Players.LocalPlayer
getgenv().AutoPedestalManager=getgenv().AutoPedestalManager or{Enabled=false,Delay=0.5,PlaceArg=0,PickupArg=1,ToolWeightAttribute="kg",SelectedRarities={},}local State=getgenv().AutoPedestalManager
local RARITY_ORDER={"common","uncommon","rare","epic","legendary","mythic","divine","eternal","transcendent",}local function Capitalize(s)return s:sub(1,1):upper()..s:sub(2)end
local RarityLabels={}local RarityDropdownValues={}local RarityIndex={}for i,rarity in ipairs(RARITY_ORDER)do
local label=Capitalize(rarity)table.insert(RarityDropdownValues,label)RarityLabels[label]=rarity
RarityIndex[rarity]=i
end
local Items=nil
local ItemsResolveFailCount=0
local function ensureItemsResolved()if Items then
return true
end
if ItemsResolveFailCount>=3 then
return false
end
local okFind,mod=pcall(function()return ReplicatedStorage:WaitForChild("TS",5):WaitForChild("constants",5):WaitForChild("items",5):WaitForChild("Items",5)end)if not okFind or not mod then
ItemsResolveFailCount+=1
return false
end
local okReq,result=pcall(require,mod)if not okReq or type(result)~="table" then
ItemsResolveFailCount+=1
return false
end
Items=result.Items or result
return Items~=nil
end
local function GetItemRarity(itemId)if not ensureItemsResolved()then
return nil
end
if type(itemId)~="string" then
return nil
end
local itemData=Items[itemId]if not itemData then
return nil
end
return itemData.rarity
end
local function RarityPassesFilter(rarity)if next(State.SelectedRarities)==nil then
return true
end
if not rarity then
return false
end
return State.SelectedRarities[rarity]==true
end
local function IsStronger(rarityIndexA,kgA,rarityIndexB,kgB)if rarityIndexA~=rarityIndexB then
return rarityIndexA>rarityIndexB
end
return kgA>kgB
end
local function GetOwnPlot()local Plots=Workspace:FindFirstChild("Plots")if not Plots then
return nil
end
for _,plot in ipairs(Plots:GetChildren())do
if plot:IsA("Model")and plot:GetAttribute("OwnerUserId")==LocalPlayer.UserId then
return plot
end
end
return nil
end
local function GetPedestals(plot)local inner=plot:FindFirstChild("Plot")local pedestalsFolder=inner and inner:FindFirstChild("Pedestals")if not pedestalsFolder then
return{}end
return pedestalsFolder:GetChildren()end
local function GetPedestalStates()local plot=GetOwnPlot()if not plot then
return{}end
local states={}for _,pedestal in ipairs(GetPedestals(plot))do
if pedestal:IsA("Model")then
local slot=pedestal:GetAttribute("Slot")local itemId=pedestal:GetAttribute("ItemId")local itemUid=pedestal:GetAttribute("ItemUid")local kg=pedestal:GetAttribute("Kg")if type(slot)=="number" then
local isEmpty=(itemId==nil or itemId=="")local rarityIndex=0
if not isEmpty then
local rarity=GetItemRarity(itemId)rarityIndex=(rarity and RarityIndex[rarity])or 0
end
table.insert(states,{pedestal=pedestal,slot=slot,isEmpty=isEmpty,itemUid=(not isEmpty and itemUid)or nil,rarityIndex=rarityIndex,kg=(not isEmpty and type(kg)=="number" and kg)or 0,})end
end
end
return states
end
local function GetCandidateTools(placedUids)local results={}local seen={}local function scan(container)if not container then
return
end
for _,tool in ipairs(container:GetChildren())do
if tool:IsA("Tool")then
local inventoryId=tool:GetAttribute("inventoryId")if type(inventoryId)=="string" and inventoryId~=""
and not seen[inventoryId]and not placedUids[inventoryId]then
if tool:GetAttribute("dirty")==false then
local itemId=tool:GetAttribute("itemId")local rarity=GetItemRarity(itemId)if rarity and RarityPassesFilter(rarity)then
seen[inventoryId]=true
local kg=tool:GetAttribute(State.ToolWeightAttribute)table.insert(results,{tool=tool,inventoryId=inventoryId,rarityIndex=RarityIndex[rarity],kg=(type(kg)=="number" and kg)or 0,})end
end
end
end
end
end
local backpack=LocalPlayer:FindFirstChildOfClass("Backpack")scan(backpack)scan(LocalPlayer.Character)return results
end
local PedestalFunctions=ReplicatedStorage:FindFirstChild("shared/network/PedestalNetwork@GlobalPedestalFunctions")local function TryPlace(slot,inventoryId)if not PedestalFunctions then
return false
end
local placeItemRemote=PedestalFunctions:FindFirstChild("placeItem")if not placeItemRemote then
return false
end
local ok=pcall(function()placeItemRemote:FireServer(State.PlaceArg,slot,inventoryId)end)return ok
end
local function TryPickup(slot)if not PedestalFunctions then
return false
end
local pickupItemRemote=PedestalFunctions:FindFirstChild("pickupItem")if not pickupItemRemote then
return false
end
local ok=pcall(function()pickupItemRemote:FireServer(State.PickupArg,slot)end)return ok
end
task.spawn(function()while true do
task.wait(State.Delay)if not State.Enabled then
continue
end
local pedestalStates=GetPedestalStates()if #pedestalStates==0 then
continue
end
table.sort(pedestalStates,function(a,b)if a.rarityIndex~=b.rarityIndex then
return a.rarityIndex<b.rarityIndex
end
return a.kg<b.kg
end)local placedUids={}for _,state in ipairs(pedestalStates)do
if state.itemUid then
placedUids[state.itemUid]=true
end
end
local candidates=GetCandidateTools(placedUids)if #candidates==0 then
continue
end
table.sort(candidates,function(a,b)if a.rarityIndex~=b.rarityIndex then
return a.rarityIndex>b.rarityIndex
end
return a.kg>b.kg
end)local candidateIdx=1
for _,state in ipairs(pedestalStates)do
if not State.Enabled then
break
end
local candidate=candidates[candidateIdx]if not candidate then
break
end
local candidateIsStronger=IsStronger(candidate.rarityIndex,candidate.kg,state.rarityIndex,state.kg)if not candidateIsStronger then
break
end
if not state.isEmpty then
TryPickup(state.slot)task.wait(0.2)end
TryPlace(state.slot,candidate.inventoryId)candidateIdx+=1
task.wait(State.Delay)end
end
end)MiscTab:Dropdown({Title="Rarity Filter",Values=RarityDropdownValues,Value={},Multi=true,Locked=false,Flag="auto_pedestal_manager_rarity_filter",Callback=function(selected)local set={}for _,label in ipairs(selected)do
local rarity=RarityLabels[label]if rarity then
set[rarity]=true
end
end
State.SelectedRarities=set
end,})MiscTab:Toggle({Title="Auto Plce Item",Desc="automatically place best item by rarity & kg",Default=State.Enabled,Flag="auto_pedestal_manager_toggle",Callback=function(value)State.Enabled=value
end,})local FPSSection=UtilityTab:Section({Title="FPS Boost",Icon="solar:rocket-2-bold",IconColor=Color3.fromRGB(120,140,255),TextSize=18,Box=true,BoxBorder=true,Opened=true,})local TeleportService=game:GetService("TeleportService")local Players=game:GetService("Players")local targetJobId=""
FPSSection:Input({Title="Place Job ID",Flag="job_id_input",Callback=function(text)targetJobId=text
end})FPSSection:Button({Title="Join Job ID",Callback=function()if targetJobId~="" then
local success,err=pcall(function()TeleportService:TeleportToPlaceInstance(game.PlaceId,targetJobId,Players.LocalPlayer)end)if not success then
end
else
end
end})FPSSection:Button({Title="Copy Job ID",Callback=function()setclipboard(game.JobId)end})local Players=game:GetService("Players")local LocalPlayer=Players.LocalPlayer
local RunService=game:GetService("RunService")local UserInputService=game:GetService("UserInputService")local VIM=game:GetService("VirtualInputManager")local antiAfkEnabled=true
local clickInterval=800
local lastClickTime=0
local antiAfkConnection=nil
local function getCenterScreen()local vp=workspace.CurrentCamera.ViewportSize
return Vector2.new(math.floor(vp.X/2)-260,math.floor(vp.Y/2)+130)end
local function realScreenClick()local center=getCenterScreen()pcall(function()VIM:SendMouseButtonEvent(center.X,center.Y,0,true,game,0)task.wait(0.1)VIM:SendMouseButtonEvent(center.X,center.Y,0,false,game,0)end)pcall(function()VIM:SendTouchEvent(center.X,center.Y,Enum.UserInputType.Touch,Enum.UserInputState.Begin,1)task.wait(0.1)VIM:SendTouchEvent(center.X,center.Y,Enum.UserInputType.Touch,Enum.UserInputState.End,1)end)end
local function resetAfkState()pcall(function()VIM:SendKeyEvent(true,Enum.KeyCode.Space,false,game)task.wait(0.1)VIM:SendKeyEvent(false,Enum.KeyCode.Space,false,game)end)end
local function startAntiAfkLoop()if antiAfkConnection then
return
end
antiAfkConnection=true
task.spawn(function()local lastResetTime=0
while antiAfkEnabled do
task.wait(1)if not antiAfkEnabled then
break
end
local now=tick()if now-lastClickTime>=clickInterval then
lastClickTime=now
realScreenClick()end
if now-lastResetTime>=600 then
lastResetTime=now
resetAfkState()end
end
antiAfkConnection=nil
end)end
local function stopAntiAfkLoop()antiAfkEnabled=false
antiAfkConnection=nil
end
local Toggle=FPSSection:Toggle({Title="Anti AFK",Value=true,Flag="anti_afk",Callback=function(state)antiAfkEnabled=state
lastClickTime=tick()if state then
startAntiAfkLoop()else
stopAntiAfkLoop()end
end,})UtilityTab:Divider()local FPS2Section=UtilityTab:Section({Title="3D Rendering",Icon="box",IconColor=Color3.fromRGB(120,140,255),TextSize=18,Box=true,BoxBorder=true,Opened=true,})local RunService=game:GetService("RunService")local G=getgenv()G.__JIN_NO3D_ON=(G.__JIN_NO3D_ON~=nil)and G.__JIN_NO3D_ON or false
G.__JIN_NO3D_TOGGLE=G.__JIN_NO3D_TOGGLE
local function notify(msg,icon,dur)if WindUI then
WindUI:Notify({Title="3D Render",Content=tostring(msg),Icon=icon or "monitor",Duration=dur or 1.2})end
end
local function apply3DDisabled(on)G.__JIN_NO3D_ON=on
local t=G.__JIN_NO3D_TOGGLE
if t then
pcall(function()if t.Set then
t:Set(on)elseif t.SetValue then
t:SetValue(on)elseif t.Toggle then
t:Toggle(on)elseif t.Update then
t:Update(on)end
end)end
if on then
pcall(function()RunService:Set3dRenderingEnabled(false)end)else
pcall(function()RunService:Set3dRenderingEnabled(true)end)end
end
do
local toggle=FPS2Section:Toggle({Title="Disable 3D Rendering",Flag="Disable_Rendering",Default=G.__JIN_NO3D_ON,Callback=function(state)apply3DDisabled(state)end,})G.__JIN_NO3D_TOGGLE=toggle
pcall(function()if toggle then
if toggle.Set then
toggle:Set(G.__JIN_NO3D_ON)elseif toggle.SetValue then
toggle:SetValue(G.__JIN_NO3D_ON)elseif toggle.Toggle then
toggle:Toggle(G.__JIN_NO3D_ON)elseif toggle.Update then
toggle:Update(G.__JIN_NO3D_ON)end
end
end)end
apply3DDisabled(G.__JIN_NO3D_ON)local Players=game:GetService("Players")local LP=Players.LocalPlayer
local GUI_NAME="JinHub_BlackScreen"
local function getPlayerGui()return LP:FindFirstChildOfClass("PlayerGui")or LP:WaitForChild("PlayerGui")end
local function ensureOverlay()local pg=getPlayerGui()local gui=pg:FindFirstChild(GUI_NAME)if not gui then
gui=Instance.new("ScreenGui")gui.Name=GUI_NAME
gui.ResetOnSpawn=false
gui.IgnoreGuiInset=true
gui.DisplayOrder=99999
gui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
gui.Enabled=false
gui.Parent=pg
local blocker=Instance.new("TextButton")blocker.Name="Shade"
blocker.BackgroundColor3=Color3.new(0,0,0)blocker.BackgroundTransparency=0
blocker.BorderSizePixel=0
blocker.Text=""
blocker.AutoButtonColor=false
blocker.Modal=true
blocker.Active=true
blocker.Selectable=false
blocker.Size=UDim2.fromScale(1,1)blocker.Position=UDim2.fromScale(0,0)blocker.ZIndex=100000
blocker.Parent=gui
local hint=Instance.new("TextLabel")hint.Name="Hint"
hint.BackgroundTransparency=1
hint.AnchorPoint=Vector2.new(0,1)hint.Position=UDim2.new(0,10,1,-10)hint.Size=UDim2.new(0,320,0,20)hint.ZIndex=100001
hint.TextXAlignment=Enum.TextXAlignment.Left
hint.TextYAlignment=Enum.TextYAlignment.Center
hint.Font=Enum.Font.GothamSemibold
hint.TextSize=13
hint.TextColor3=Color3.fromRGB(160,160,160)hint.Text="Black Screen ON — turn off the toggle to restore"
hint.Parent=gui
end
return gui
end
local function setBlack(on)local gui=ensureOverlay()gui.Enabled=on and true or false
end
FPS2Section:Toggle({Title="Black Screen",Flag="Black_Screen",Desc="Hide Everything",Default=false,Callback=function(on)setBlack(on)if WindUI then
end
end,})LP.CharacterAdded:Connect(function()task.defer(function()local gui=ensureOverlay()end)end)UtilityTab:Divider()local FPS3Section=UtilityTab:Section({Title="Player Utility",Icon="solar:user-bold",IconColor=Color3.fromRGB(120,140,255),TextSize=18,Box=true,BoxBorder=true,Opened=true,})local player=game.Players.LocalPlayer
local character=player.Character or player.CharacterAdded:Wait()local humanoid=character:WaitForChild("Humanoid")local walkspeedEnabled=false
local walkspeedValue=70
local originalWalkspeed=humanoid.WalkSpeed
local walkspeedConnection=nil
local function updateWalkspeed()if walkspeedEnabled then
humanoid.WalkSpeed=walkspeedValue
else
humanoid.WalkSpeed=originalWalkspeed
end
end
local function startWalkspeedLoop()if walkspeedConnection then
return
end
walkspeedConnection=true
task.spawn(function()while walkspeedEnabled do
task.wait(0.5)if not walkspeedEnabled then
break
end
if humanoid and humanoid.WalkSpeed~=walkspeedValue then
humanoid.WalkSpeed=walkspeedValue
end
end
walkspeedConnection=nil
end)end
local function stopWalkspeedLoop()walkspeedEnabled=false
walkspeedConnection=nil
end
player.CharacterAdded:Connect(function(char)character=char
humanoid=char:WaitForChild("Humanoid")originalWalkspeed=humanoid.WalkSpeed
updateWalkspeed()if walkspeedEnabled then
startWalkspeedLoop()end
end)local Toggle=FPS3Section:Toggle({Title="Enable Walkspeed",Flag="Enable_Walkspeed",Value=false,Callback=function(state)walkspeedEnabled=state
updateWalkspeed()if state then
startWalkspeedLoop()else
stopWalkspeedLoop()end
end,})local Slider=FPS3Section:Slider({Title="Walkspeed Boost",Flag="Walkspeed_boost",Step=1,Value={Min=16,Max=300,Default=26,},Callback=function(value)walkspeedValue=value
if walkspeedEnabled then
humanoid.WalkSpeed=value
end
end,})local infiniteJumpEnabled=false
local userInputService=game:GetService("UserInputService")local function onJumpRequest()if infiniteJumpEnabled and humanoid then
humanoid:ChangeState(Enum.HumanoidStateType.Jumping)end
end
local ToggleInfiniteJump=FPS3Section:Toggle({Title="Enable Infinite Jump",Value=false,Flag="Enable_Infinite",Callback=function(state)infiniteJumpEnabled=state
end,})userInputService.JumpRequest:Connect(onJumpRequest)local fullbrightEnabled=false
local originalLighting={}local lighting=game:GetService("Lighting")local lightingConnection
local function saveOriginalLighting()if next(originalLighting)==nil then
originalLighting.Ambient=lighting.Ambient
originalLighting.ColorShift_Bottom=lighting.ColorShift_Bottom
originalLighting.ColorShift_Top=lighting.ColorShift_Top
originalLighting.OutdoorAmbient=lighting.OutdoorAmbient
originalLighting.Brightness=lighting.Brightness
originalLighting.ClockTime=lighting.ClockTime
originalLighting.FogEnd=lighting.FogEnd
originalLighting.GlobalShadows=lighting.GlobalShadows
end
end
local function enableFullbright()saveOriginalLighting()lighting.Ambient=Color3.new(1,1,1)lighting.ColorShift_Bottom=Color3.new(1,1,1)lighting.ColorShift_Top=Color3.new(1,1,1)lighting.OutdoorAmbient=Color3.new(1,1,1)lighting.Brightness=2
lighting.ClockTime=14
lighting.FogEnd=100000
lighting.GlobalShadows=false
end
local function disableFullbright()if next(originalLighting)~=nil then
lighting.Ambient=originalLighting.Ambient
lighting.ColorShift_Bottom=originalLighting.ColorShift_Bottom
lighting.ColorShift_Top=originalLighting.ColorShift_Top
lighting.OutdoorAmbient=originalLighting.OutdoorAmbient
lighting.Brightness=originalLighting.Brightness
lighting.ClockTime=originalLighting.ClockTime
lighting.FogEnd=originalLighting.FogEnd
lighting.GlobalShadows=originalLighting.GlobalShadows
end
end
local ToggleFullbright=FPS3Section:Toggle({Title="Enable Fullbright",Value=false,Flag="Enable_Fullbright",Callback=function(state)fullbrightEnabled=state
if state then
enableFullbright()lightingConnection=lighting.Changed:Connect(function()if fullbrightEnabled then
task.wait()enableFullbright()end
end)else
if lightingConnection then
lightingConnection:Disconnect()lightingConnection=nil
end
disableFullbright()end
end,})local HttpService=game:GetService("HttpService")local BaseFolder=Window.Folder
local ConfigFolder=BaseFolder.."/configs"
local IndexFile=ConfigFolder.."/index.json"
local AutoLoadFile=BaseFolder.."/autoload.txt"
if makefolder then
if not isfolder(BaseFolder)then
makefolder(BaseFolder)end
if not isfolder(ConfigFolder)then
makefolder(ConfigFolder)end
end
local ConfigName=""
local SelectedConfig=nil
local ConfigDropdown
local AutoLoadButton
local function Notify(title,text,color)WindUI:Notify({Title=title,Content=text,Duration=3,Color=color or Color3.fromRGB(100,255,100),})end
local function LoadIndex()if isfile and isfile(IndexFile)then
local ok,data=pcall(function()return HttpService:JSONDecode(readfile(IndexFile))end)if ok and type(data)=="table" then
return data
end
end
return{}end
local function SaveIndex(t)if writefile then
writefile(IndexFile,HttpService:JSONEncode(t))end
end
local function HasConfig(name)for _,v in ipairs(LoadIndex())do
if v==name then
return true
end
end
return false
end
local function GetAutoLoad()if isfile and isfile(AutoLoadFile)then
return readfile(AutoLoadFile)end
return ""
end
local function UpdateAutoLoadDesc()local name=GetAutoLoad()if AutoLoadButton then
AutoLoadButton:SetDesc(name~="" and("Auto Load : "..name)or "No Auto Load Config")end
end
ConfigTab:Input({Title="Config Name",Callback=function(text)ConfigName=text
end,})ConfigDropdown=ConfigTab:Dropdown({Title="Select Config",Values=LoadIndex(),Multi=false,Callback=function(v)SelectedConfig=v
ConfigName=v
end,})local function UpdateDropdown()if ConfigDropdown and ConfigDropdown.Refresh then
ConfigDropdown:Refresh(LoadIndex())end
end
local ConfigGroup=ConfigTab:Group({})ConfigGroup:Button({Title="Create Config",Callback=function()if ConfigName=="" then
Notify("Error","Config empty",Color3.fromRGB(255,100,100))return
end
if HasConfig(ConfigName)then
Notify("Error","Config already exist",Color3.fromRGB(255,100,100))return
end
Window.ConfigManager:Config(ConfigName):Save()local list=LoadIndex()table.insert(list,ConfigName)SaveIndex(list)UpdateDropdown()SelectedConfig=ConfigName
task.defer(function()if ConfigDropdown and ConfigDropdown.Set then
ConfigDropdown:Set(ConfigName)end
end)Notify("Success","Config created: "..ConfigName)end,})ConfigGroup:Button({Title="Overwrite Config",Callback=function()if not SelectedConfig then
Notify("Error","selected config fist!",Color3.fromRGB(255,100,100))return
end
Window.ConfigManager:Config(SelectedConfig):Save()Notify("Success","Overwrite: "..SelectedConfig)end,})ConfigTab:Button({Title="Load Config",Callback=function()if not SelectedConfig then
Notify("Error","selected config fist!",Color3.fromRGB(255,100,100))return
end
Window.ConfigManager:Config(SelectedConfig):Load()Notify("Success","Loaded: "..SelectedConfig)end,})AutoLoadButton=ConfigTab:Button({Title="Set Auto Load",Desc="No Auto Load Config",Callback=function()if not SelectedConfig then
Notify("Error","selected config fist!",Color3.fromRGB(255,100,100))return
end
writefile(AutoLoadFile,SelectedConfig)UpdateAutoLoadDesc()Notify("Success","Auto load set: "..SelectedConfig)end,})ConfigTab:Button({Title="Reset Auto Load",Callback=function()writefile(AutoLoadFile,"")UpdateAutoLoadDesc()Notify("Success","Auto load delete")end,})ConfigTab:Button({Title="Remove Selected",Callback=function()if not SelectedConfig then
Notify("Error","select config first!",Color3.fromRGB(255,100,100))return
end
local list=LoadIndex()for i,v in ipairs(list)do
if v==SelectedConfig then
table.remove(list,i)break
end
end
SaveIndex(list)if GetAutoLoad()==SelectedConfig then
writefile(AutoLoadFile,"")end
SelectedConfig=nil
ConfigName=""
UpdateDropdown()UpdateAutoLoadDesc()Notify("Success","Config deleted")end,})ConfigTab:Button({Title="Remove All",Callback=function()SaveIndex({})writefile(AutoLoadFile,"")SelectedConfig=nil
ConfigName=""
UpdateDropdown()UpdateAutoLoadDesc()Notify("Success","all config deleted!")end,})task.spawn(function()task.wait(1)local auto=GetAutoLoad()if auto~="" then
pcall(function()Window.ConfigManager:Config(auto):Load()end)Notify("Success","Auto loaded: "..auto)end
UpdateAutoLoadDesc()end)
